#!/usr/bin/env python

# import sys
# import logging

from endpoints.interface.wsgi import Application, Server
from datatypes import logging


logging.quick_config()

# logging.basicConfig(
#     format="%(message)s",
#     level=logging.DEBUG,
#     stream=sys.stdout
# )
# sys.path.insert(0, "")

with Server(application=Application("bounce.controllers")) as s:
    s.serve_forever()

# Server(
#     application=Application("bounce.controllers")
# ).serve_forever()


# from bounce import server
# 
# server.debug = True
# server.run()

