#!/usr/bin/env python

import sys
import logging

logging.basicConfig(format="%(message)s", level=logging.DEBUG, stream=sys.stdout)
sys.path.insert(0, "")

from bounce import server

server.debug = True
server.run()

